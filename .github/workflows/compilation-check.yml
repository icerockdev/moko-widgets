name: KMP library compilation check

on:
  pull_request:
    branches:
      - master
      - develop

jobs:
  plugin-build:
    runs-on: macOS-latest

    steps:
      - uses: actions/checkout@v1
      - name: Set up JDK 1.8
        uses: actions/setup-java@v1
        with:
          java-version: 1.8
      - uses: actions/cache@v1
        name: Cache ~/.m2/repository
        with:
          path: ~/.m2/repository
          key: ${{ runner.os }}-maven
      - uses: actions/cache@v1
        name: Cache ~/.konan
        with:
          path: ~/.konan
          key: ${{ runner.os }}-konan
      - name: Check plugin
        run: ./gradlew -PpluginPublish publishPluginPublicationToMavenLocal

  core-build:
    runs-on: macOS-latest
    needs: plugin-build

    steps:
      - uses: actions/checkout@v1
      - name: Set up JDK 1.8
        uses: actions/setup-java@v1
        with:
          java-version: 1.8
      - uses: actions/cache@v1
        name: Cache ~/.m2/repository
        with:
          path: ~/.m2/repository
          key: ${{ runner.os }}-maven
      - uses: actions/cache@v1
        name: Cache ~/.konan
        with:
          path: ~/.konan
          key: ${{ runner.os }}-konan
      - name: Check core
        run: ./gradlew -PcorePublish :widgets:publishToMavenLocal

  additions-flat-build:
    runs-on: macOS-latest
    needs: core-build

    steps:
      - uses: actions/checkout@v1
      - name: Set up JDK 1.8
        uses: actions/setup-java@v1
        with:
          java-version: 1.8
      - uses: actions/cache@v1
        name: Cache ~/.m2/repository
        with:
          path: ~/.m2/repository
          key: ${{ runner.os }}-maven
      - uses: actions/cache@v1
        name: Cache ~/.konan
        with:
          path: ~/.konan
          key: ${{ runner.os }}-konan
      - name: Cocoapods install
        run: (cd sample/ios-app && pod install)
      - name: Check flat
        run: ./gradlew -PadditionsPublish :widgets-flat:publishToMavenLocal

  additions-bottomsheet-build:
    runs-on: macOS-latest
    needs: core-build

    steps:
      - uses: actions/checkout@v1
      - name: Set up JDK 1.8
        uses: actions/setup-java@v1
        with:
          java-version: 1.8
      - uses: actions/cache@v1
        name: Cache ~/.m2/repository
        with:
          path: ~/.m2/repository
          key: ${{ runner.os }}-maven
      - uses: actions/cache@v1
        name: Cache ~/.konan
        with:
          path: ~/.konan
          key: ${{ runner.os }}-konan
      - name: Cocoapods install
        run: (cd sample/ios-app && pod install)
      - name: Check bottomsheet
        run: ./gradlew -PadditionsPublish :widgets-bottomsheet:publishToMavenLocal

  additions-sms-build:
    runs-on: macOS-latest
    needs: core-build

    steps:
      - uses: actions/checkout@v1
      - name: Set up JDK 1.8
        uses: actions/setup-java@v1
        with:
          java-version: 1.8
      - uses: actions/cache@v1
        name: Cache ~/.m2/repository
        with:
          path: ~/.m2/repository
          key: ${{ runner.os }}-maven
      - uses: actions/cache@v1
        name: Cache ~/.konan
        with:
          path: ~/.konan
          key: ${{ runner.os }}-konan
      - name: Cocoapods install
        run: (cd sample/ios-app && pod install)
      - name: Check sms
        run: ./gradlew -PadditionsPublish :widgets-sms:publishToMavenLocal

  additions-datetime-picker-build:
    runs-on: macOS-latest
    needs: core-build

    steps:
      - uses: actions/checkout@v1
      - name: Set up JDK 1.8
        uses: actions/setup-java@v1
        with:
          java-version: 1.8
      - uses: actions/cache@v1
        name: Cache ~/.m2/repository
        with:
          path: ~/.m2/repository
          key: ${{ runner.os }}-maven
      - uses: actions/cache@v1
        name: Cache ~/.konan
        with:
          path: ~/.konan
          key: ${{ runner.os }}-konan
      - name: Cocoapods install
        run: (cd sample/ios-app && pod install)
      - name: Check datetime-picker
        run: ./gradlew -PadditionsPublish :widgets-datetime-picker:publishToMavenLocal

  additions-collection-build:
    runs-on: macOS-latest
    needs: core-build

    steps:
      - uses: actions/checkout@v1
      - name: Set up JDK 1.8
        uses: actions/setup-java@v1
        with:
          java-version: 1.8
      - uses: actions/cache@v1
        name: Cache ~/.m2/repository
        with:
          path: ~/.m2/repository
          key: ${{ runner.os }}-maven
      - uses: actions/cache@v1
        name: Cache ~/.konan
        with:
          path: ~/.konan
          key: ${{ runner.os }}-konan
      - name: Cocoapods install
        run: (cd sample/ios-app && pod install)
      - name: Check collection
        run: ./gradlew -PadditionsPublish :widgets-collection:publishToMavenLocal

  additions-image-network-build:
    runs-on: macOS-latest
    needs: core-build

    steps:
      - uses: actions/checkout@v1
      - name: Set up JDK 1.8
        uses: actions/setup-java@v1
        with:
          java-version: 1.8
      - uses: actions/cache@v1
        name: Cache ~/.m2/repository
        with:
          path: ~/.m2/repository
          key: ${{ runner.os }}-maven
      - uses: actions/cache@v1
        name: Cache ~/.konan
        with:
          path: ~/.konan
          key: ${{ runner.os }}-konan
      - name: Cocoapods install
        run: (cd sample/ios-app && pod install)
      - name: Check image network
        run: ./gradlew -PadditionsPublish :widgets-image-network:publishToMavenLocal